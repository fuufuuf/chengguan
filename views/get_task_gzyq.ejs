<html>
<head>
    <%- include('header') -%>

</head>
<body>

    <!--/延期挂账-->
    <div class="easyui-tabs" style="height:800px">

        <div title="已批复延期" style="padding:10px">
        <table id="guazhang_1" title="" class="easyui-datagrid" style="height:800px" rownumbers="true"
        url="accredit?status=1" pagination="true" fitColumns="true" singleSelect="true">
        <thead>
        <tr>
        <th field="TaskNum">任务编号</th>
        <th field="ReplyDate">申请时间</th>
        <th field="ReplyDate">答复时间</th>
        <th field="ApplyFlag">申请类型</th>
        <th field="ReplyInfo">申请结果</th>
        <th field="ReplyMemo">答复意见</th>
        <th field="operation" data-options="
        formatter:function(value,row,index){

        return $('#opbar2').html();

        }">
        任务操作
        </th>
        </tr>
        </thead>
        </table>
        <div id="opbar2" style="display:none;">
        <a href="#" class="easyui-linkbutton" iconCls="icon-add" plain="true" onclick="showDetail('#guazhang_0')">详细信息</a>
        </div>
        </div>

        <div title="未批复延期" style="padding:10px">
            <table id="guazhang_0" title="" class="easyui-datagrid" style="height:800px" rownumbers="true"
                   url="accredit?status=0" pagination="true" fitColumns="true" singleSelect="true">
                <thead>
                <tr>
                    <th field="TaskNum">任务编号</th>
                    <th field="ApplyOpter">申请人</th>
                    <th field="ApplyDate">申请时间</th>
                    <th field="ApplyFlag">申请类型</th>
                    <th field="ApplyDelayInfo">延期时间</th>
                    <th field="ApplyMemo">申请理由</th>
                    <th field="operation" data-options="
            formatter:function(value,row,index){

                    return $('#opbar').html();

            }">
                        任务操作
                    </th>
                </tr>
                </thead>
                <div id="opbar" style="display:none;">
                    <a href="#" class="easyui-linkbutton" iconCls="icon-add" plain="true" onclick="showDetail('#guazhang_0')">详细信息</a>
                </div>
            </table>
        </div>
    </div>

<!--延期挂账-->

</body>
<script>

    function showDetail(id){
        var url;

        var row = $(id).datagrid('getSelected');

        if(row){

            url = 'task_details?id='+row.TaskNum;
            location.href = url;//location.href实现客户端页面的跳转
        }


    }

/*format date*/
    Date.prototype.Format = function(fmt)
    { //author: meizz
        var o = {
            "M+" : this.getMonth()+1,                 //月份
            "d+" : this.getDate(),                    //日
            "h+" : this.getHours(),                   //小时
            "m+" : this.getMinutes(),                 //分
            "s+" : this.getSeconds(),                 //秒
            "q+" : Math.floor((this.getMonth()+3)/3), //季度
            "S"  : this.getMilliseconds()             //毫秒
        };
        if(/(y+)/.test(fmt))
            fmt=fmt.replace(RegExp.$1, (this.getFullYear()+"").substr(4 - RegExp.$1.length));
        for(var k in o)
            if(new RegExp("("+ k +")").test(fmt))
                fmt = fmt.replace(RegExp.$1, (RegExp.$1.length==1) ? (o[k]) : (("00"+ o[k]).substr((""+ o[k]).length)));
        return fmt;
    }

</script>
</html>